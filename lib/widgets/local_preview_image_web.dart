// Web has no local file path preview, so callers fall back to network sources.

import 'package:flutter/material.dart';

Widget? localPreviewImage({
  required String path,
  required ImageErrorWidgetBuilder errorBuilder,
}) {
  return null;
}
